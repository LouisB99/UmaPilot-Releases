Keeps the published version attached to future portable packages, so their updater never tries to reapply a release already included in the package. Adds guards against moving publisher history backwards.

Existing updater-enabled installations download this small patch automatically. For the original September 9 ZIP, use UmaPilot-Enable-Updates.zip once; your saved data stays in place.
